Prims1: MST from list

Prims2: MST from matrix

b versions are with min-heap for queue

OUTPUT: MST
                                                                            times
getMST(AdjList, root node)
    While [Q is not empty]                                                      n                                                
        u = extractMin()                                                        
        add u to MST
        for each node adj to u                                                  n-1
            for each node in queue                                              length of queue
                if [adj.node is in queue && has smaller weight]
                    queue.node.key = newkey
                    queue.node.parent = u
                    fixHeap(index of queue.node)   //if using minheap


                    O(E log(V)) V = n, E = n*(n-1)/2
                    O( 0.5n(n-1) log(n) )
                    O( n^2 log(n) )

getMST(AdjMatrix, root node)
    While [Q is not empty]                                                      n                                                
        u = extractMin()                                                        
        add u to MST
        for each node adj to u                                                  n
            for each node in queue                                              length of queue
                if [adj.node is in queue && has smaller weight]
                    queue.node.key = newkey
                    queue.node.parent = u
                    fixHeap(index of queue.node)   //if using minheap                    
    
extractMin()    //for unsorted list
    for each node in queue                              
        find node with smallest key           
    remove from queue
    return node

extractMin()    //for min-heap                                                  1
    remove heap-root node from queue
    return node

fixHeap(index)                                                                  log2(length of queue)                                                                 
    if [not at heap-root]
        if [parent.key > index.key]
            swap parent and child
            fixHeap(parent.index)


outputAdjList











